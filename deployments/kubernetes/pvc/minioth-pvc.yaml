apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minioth-pvc
  namespace: kuspace
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: kuspace-default
  resources:
    requests:
      storage: 1Gi

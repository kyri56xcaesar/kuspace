apiVersion: v1
kind: Service
metadata:
  name: uspace-nodeport
  labels:
    app: uspace
  namespace: kuspace
spec:
  type: NodePort
  selector:
    app: uspace
  ports:
    - name: uspace
      protocol: TCP
      port: 8079
      targetPort: 8079
      nodePort: 30079
      
